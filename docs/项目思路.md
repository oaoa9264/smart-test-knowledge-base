





想法：需求的工作流需要整个团队一起维护，不能依靠ai、或者某个人





目前规则树痛点：针对老需求，新的需求进来，怎么方便看差异，怎么知道这个变动对不对



想法：需求A，版本1、版本2、版本3 需要看到之前的版本是什么，加个一个对比，比如我版本1和版本2进行对比，能够分析出来这个需求A，新老版本变化





痛点：

同样一个需求，规则树用llm模型分析出来，内容不太一样，但是我有一个思路，用llm模型分析两次，然后两次内容进行融合，融合出来最合理的规则树，后续如果这个需求版本A有变动，变成需求版本B，那我就以版本B的需求加上版本A的规则树的节点内容



想法：当我用llm生成规则树之后，我希望这个ai对话历史能够保留下来，比如我需求a版本1生成了规则树，已经确定规则树，那么下次当我需求a有变动的时候，我可以参考需求a版本1的对话历史，结合新的需求，生成需求a版本2的规则树，ai获取需求a版本1规则树的上下文，如果版本2部分需求有变更，那么规则树只用改变动的部分，其他未变动的规则还保持和版本1规则一致





想法：规则树可以导入、导出，可以按照规则树生成测试方案，然后通过测试方案生成测试用例，测试用例绑定规则树，形成闭环

规则树需要人工参与修改，并且规则树支持导入



想法：我还是希望规则树引入流程图控件，哪怕这个项目需要大改，xmind-sdk-js，你了解这个吗



想法：测试用例，加入测试方案，测试方案是根据规则流程生成的，然后根据测试方案生成测试用例，绑定规则流程图的节点引入

测试方案和测试用例都是通过：xmind控件展示，支持xmind导入展示，也支持xmind导出







当某个项目下需求有变动的时候，生成新的规则树，原本的规则树和现在新生成的规则树进行对比，相同的数据流进行保留，新生成的规则树新的数据流替换掉旧的数据流，但是旧的数据流不能删除，当做证据留存，方便后续用户查看的时候，能够看到之前的逻辑是什么





当我只有技能树，没有测试用例，我想要做一个自动导入测试用例的功能，但是我测试用例是通过excel导入或者思维导图导入，能不能通过llm根据我导入的用例自己去匹配节点？





我的测试用例是不是也可以做成XMind形式展现？







因为我这个项目肯定有会不同的需求，当我需求产生的依赖图足够覆盖整个项目的时候，通过ai把他们都整合到一起，形成完整的项目的数据流，当然这个项目的数据流会很大，但是我们可以把它拆开，变成某个产品的数据流







我目前对这个项目的想法，最开始先收集数据，收集需求的规则树，也就是数据流，有了这些数据流，我们可以做很多事情，比如可以按照现有的数据流，可以用ai去分析需求变动是否合理，变动会影响到其它的数据流。

慢慢积累测试的资产，当项目整个链路的数据流都建立起来以后，可以做很多事情